import React, { Component, PropTypes } from 'react'

export default class Customers extends Component {

  static propTypes = {}

  render () {
    return (
      <div>
        <p>I display the selected customers. I will display all the customers if none are selected</p>
      </div>
    )
  }
}
