import React, { Component, PropTypes } from 'react'

export default class Campaigns extends Component {

  static propTypes = {}

  render () {
    return (
      <div>
        <p>I display all the selected campaigns. Possibly all of them if none are selected</p>
      </div>
    )
  }
}
