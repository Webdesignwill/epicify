import { connect } from 'react-redux';
import LineItems from '../../../dashboards/mediaspecialist/LineItems';

function mapStateToProps(store, props) {
  return {}
}

export default connect(mapStateToProps)(LineItems);
